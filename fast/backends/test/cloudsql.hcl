# Remote state for stack "cloudsql" (env: test)
# Regenerate: node infra/scripts/generate-fast-stages.mjs
# Must match state_bucket in datasets/test/env.tfvars

bucket = "REPLACE_GCP_TEST_PROJECT-retail-tfstate-test"
prefix = "test/cloudsql"
